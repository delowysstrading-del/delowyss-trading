services:
  - type: web
    name: delowyss-trading-pro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python ejecutar.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /health
    autoDeploy: true